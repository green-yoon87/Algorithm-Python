# 💯 오답노트

### 💡**문제 분석 요약**

어떤 숫자에서 k개의 수를 제거했을 때 얻을 수 있는 가장 큰 숫자를 구하려 합니다.

예를 들어, 숫자 1924에서 수 두 개를 제거하면 [19, 12, 14, 92, 94, 24] 를 만들 수 있습니다. 이 중 가장 큰 숫자는 94 입니다.

문자열 형식으로 숫자 number와 제거할 수의 개수 k가 solution 함수의 매개변수로 주어집니다. number에서 k 개의 수를 제거했을 때 만들 수 있는 수 중 가장 큰 숫자를 문자열 형태로 return 하도록 solution 함수를 완성하세요.

### 💡**알고리즘 설계**

일단 숫자 문자열을 숫자 리스트로 변환했다. 

첫번째 접근 방법은 sliding window 접근으로 배열을 순회하면서 max 값을 찾는 방법을 썼다. → 시간 초과 

두번째 접근 방법은 튜플형을 이용하여 (숫자, index)로 저장하여 숫자에 대해서 내림차순 정렬을 수행하고 index에 대해서는 오름차순 정렬을 한다. 각 리스트에 대해서 큰 숫자를 대상으로 해당 값이 추가할 수 있는지 인덱스 값으로 추가한다. → 시간 초과 

여기서 핵심은 순서를 유지해야한다는 것이다! → 따라서 스택을 이용해야 한다. 

배열을 순회한다.

 stack의 위 원소가 추가하려는 원소보다 작으면 pop을 해야한다.(k가 0보다 커야할 때)

좀 더 자세한 알고리즘은 코드 참조하세요…

### 💡 느낀점 or 기억할정보

1. 순서가 유지될 때, stack을 사용해야할 확률이 높다는 것! 
2. 모든 경우의 수를 생각하자 제발 최대한 많이 할 수 있을 만큼 ..
